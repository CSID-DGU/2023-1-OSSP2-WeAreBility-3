//산책로 페이지 Trail()
import 'package:flutter/material.dart';

class Trail extends StatelessWidget {
  const Trail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('산책로'));
  }
}
