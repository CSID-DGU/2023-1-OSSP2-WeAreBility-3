//나만의 산책로 페이지 Myrail()
import 'package:flutter/material.dart';

class Myrail extends StatelessWidget {
  const Myrail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('나만의 산책로'));
  }
}
